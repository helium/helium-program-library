services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: atomic_test
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
    ports:
      - "5433:5432"
    tmpfs:
      - /var/lib/postgresql/data
