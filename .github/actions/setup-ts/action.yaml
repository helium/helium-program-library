name: "Setup ts"
description: "Setup ts"
runs:
  using: "composite"
  steps:
    - uses: actions/setup-node@v2
      with:
        node-version: ${{ env.NODE_VERSION }}
    - uses: actions/cache@v4
      name: Cache Typescript node_modules
      id: cache-typescript-node-modules
      with:
        path: |
          ./node_modules/
          ./packages/*/node_modules/
        key: node-modules-${{ runner.os }}-v0001-${{ env.NODE_VERSION }}-${{ hashFiles('./yarn.lock') }}-${{ hashFiles('./**/*/package.json') }}
    - run: yarn install
      shell: bash
      if: steps.cache-typescript-node-modules.outputs.cache-hit != 'true'
    - run: yarn run build
      shell: bash
