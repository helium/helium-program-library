name: "Setup Anchor"
description: "Setup Anchor"
inputs:
  devnet:
    description: "Whether to use devnet feature"
    required: false
    default: "false"
  testing:
    description: "Whether to build with test values (sets TESTING and the testing feature)"
    required: false
    default: "false"
  program:
    description: "The program to build"
    required: false
runs:
  using: "composite"
  steps:
    - uses: ./.github/actions/setup/
    - uses: ./.github/actions/setup-anchor/
    - uses: actions/cache@v4
      name: Cache Cargo registry + index
      id: cache-cargo-registry
      with:
        path: |
          ~/.cargo/bin/
          ~/.cargo/registry/index/
          ~/.cargo/registry/cache/
          ~/.cargo/git/db/
        key: cargo-${{ runner.os }}-cargo-build-${{ hashFiles('**/Cargo.lock') }}
    - name: Cache Anchor Build
      uses: actions/cache@v4
      id: cache-anchor-build
      with:
        path: |
          ./target/
        key: build-${{ runner.os }}-${{env.ANCHOR_VERSION}}-${{env.ANCHOR_SHA}}-v0002-${{ inputs.devnet }}-${{ inputs.testing }}-${{ hashFiles('./programs/**/**') }}-${{ inputs.program }}
    # The programs refuse to compile when TESTING is set without the `testing` feature, so
    # a test build carries both.
    - run: |
        FEATURES=""
        if [ "$DEVNET" = "true" ]; then FEATURES="devnet"; fi
        if [ "$TESTING_BUILD" = "true" ]; then
          export TESTING=true
          FEATURES="${FEATURES:+$FEATURES,}testing"
        fi
        ARGS=()
        if [ -n "$PROGRAM" ]; then ARGS+=(-p "$PROGRAM"); fi
        if [ -n "$FEATURES" ]; then ARGS+=(-- --features "$FEATURES"); fi
        ~/.cargo/bin/anchor build "${ARGS[@]}"
      if: steps.cache-anchor-build.outputs.cache-hit != 'true'
      shell: bash
      env:
        DEVNET: ${{ inputs.devnet }}
        TESTING_BUILD: ${{ inputs.testing }}
        PROGRAM: ${{ inputs.program }}
