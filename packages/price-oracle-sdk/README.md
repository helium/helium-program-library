# @helium/price-oracle-sdk

TypeScript SDK for the [price-oracle program](../../programs/price-oracle). The on-chain oracle aggregates prices submitted by a whitelisted set of oracles and exposes a median; this SDK lets oracles submit and consumers read the current price.

Note that this is deprecated and was only used post-migration when there was no pyth support.