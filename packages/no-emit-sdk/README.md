# @helium/no-emit-sdk

TypeScript SDK for the [no-emit program](../../programs/no-emit), which tracks HNT that was not-emitted (returned to the network) rather than distributed. Used by sub-DAO accounting to reduce the next epoch's issuance when a reward is forfeited.
