# @helium/lazy-transactions-sdk

TypeScript SDK for the [lazy-transactions program](../../programs/lazy-transactions), which commits a Merkle tree of pre-signed transactions on-chain so any of them can be executed later with a proof. Used heavily by [`migration-service`](../migration-service) for the HNT → Solana migration.
