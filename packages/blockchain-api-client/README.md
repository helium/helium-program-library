# @helium/blockchain-api-client

TypeScript client and Zod schemas for the [blockchain-api](../blockchain-api) service. Consumers import the typed client rather than hand-writing `fetch` calls, so request/response shapes stay in sync with the server.
