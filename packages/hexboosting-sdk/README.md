# @helium/hexboosting-sdk

TypeScript SDK for the [hexboosting program](../../programs/hexboosting), which lets hotspot owners spend DC to boost the rewards multiplier for hexes they cover over fixed periods. Exposes helpers for creating boost configs, starting/extending boosts, and closing expired boosts.

Note that hexboosting is deprecated, and no longer used.