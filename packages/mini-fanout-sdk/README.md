# @helium/mini-fanout-sdk

TypeScript SDK for the [mini-fanout program](../../programs/mini-fanout) — a rent-optimised fanout that splits a single token account across ≤ 10 wallets. Cheaper than a full fanout NFT when you need many small splits (e.g. per-hotspot reward routing).
