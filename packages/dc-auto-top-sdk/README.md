# @helium/dc-auto-top-sdk

TypeScript SDK for the [dc-auto-top program](../../programs/dc-auto-top), which lets users pre-authorise automatic HNT → DC top-ups when their data-credit balance drops below a threshold. The top-ups themselves are cranked on-chain by tuktuk.
