# docsite

Docusaurus site that hosts the Helium governance / Solana-programs developer docs (program overviews, IDL references, SDK guides). Not deployed from this repo's docker pipeline.

```sh
pnpm start    # local dev server
pnpm build    # static build under build/
```
