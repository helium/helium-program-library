---
"@helium/blockchain-api": minor
---

Add `governance.getPositions` (GET /positions/wallet/{wallet}) listing a wallet's voter-stake-registry positions with deposited amount, governing mint, and lockup info. Governance and migration contracts now also declare `RATE_LIMITED` errors.
