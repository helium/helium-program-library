{
  "$schema": "https://turbo.build/schema.json",
  "cacheDir": ".turbo",
  "tasks": {
    "prebuild": {
      "cache": true,
      "outputs": ["lib/**"]
    },
    "build": {
      "dependsOn": ["prebuild", "^build"],
      "outputs": ["lib/**", "dist/**", ".next/**", "!.next/cache/**"],
      "cache": true
    },
    "@helium/idls#build": {
      "dependsOn": ["prebuild", "^build"],
      "outputs": ["lib/**"],
      "cache": true,
      "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/target/types/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["build"],
      "cache": false
    },
    "lint": {
      "cache": true
    }
  }
}
